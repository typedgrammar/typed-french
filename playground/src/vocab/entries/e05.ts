import type { VocabEntry } from "../types";

const entries: VocabEntry[] = [
  // Only NEW content words used by e05 snippets that are not already in batch-01.
  { word: "ici", ipa: "/i.si/", pos: "adverb", en: "here", zh: "这里" },
];

export default entries;
